
language: node_js

env:
  - NODE_ENV='production'

node_js:
  - "8"

script:
  - npm install
  - npm run build
  - export token=$GH_PAGES_TOKEN
  
deploy:
  - provider: pages
  - skip-cleanup: true
  - github-token: $token
  - keep-history: true
  - on:
      branch: master
